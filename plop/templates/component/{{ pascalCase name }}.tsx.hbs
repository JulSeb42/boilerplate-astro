import clsx from "clsx"
import type { I{{>pascalName}} } from "./types"

export const {{>pascalName}} = ({ element = "{{ lowerCase tag }}" }: I{{>pascalName}}) => {
	const Element = element

	return (
		<Element className={clsx()}>
			
		</Element>
	)
}
